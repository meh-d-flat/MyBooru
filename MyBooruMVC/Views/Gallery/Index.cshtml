@using Microsoft.Extensions.Configuration;
@inject IConfiguration config
@model (int page, int reverse)
@{
    ViewBag.Title = "Index";
    Layout = "_Layout";
}
<p id="smth"></p>
<center id="pages">
</center>
<script>
    window.onload += populateGallery('@config["ApiHost"]', @Model.page, @Model.reverse);
</script>